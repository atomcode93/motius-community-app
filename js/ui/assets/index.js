// TODO: Try to import all images in a file and export the results. That way you only have to write the path to the file once. For example:

import downArrow from './down-arrow.png';
import motius from './motius.png'

export default {
    downArrow,
    motius,
}
